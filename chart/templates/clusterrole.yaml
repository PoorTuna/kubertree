{{- /*
kubertree acts as the logged-in user, never as its own ServiceAccount, so it
needs no standing cluster permissions of its own. The only binding it may need
is to the built-in system:auth-delegator role, so the oauth-proxy sidecar can
perform TokenReview/SubjectAccessReview on OpenShift — see clusterrolebinding.yaml.
*/ -}}
